# 订阅转换

将源订阅与转换选项组合为定制订阅，并支持恢复选项、生成短链接及上传远程配置。

## 领域词汇

**源订阅**：待转换的订阅地址或节点链接，可包含多条。

**定制订阅**：包含源订阅、目标客户端和转换选项的长链接。

**生成链接**：用户最近一次生成的定制订阅，以及与其对应的短链接（如有）。修改表单不会自动生成新的定制订阅。

**订阅短链接**：指向某个确定的定制订阅的短链接；不能代表之后重新生成的定制订阅。

**远程配置**：转换时使用的规则配置，其地址可以手动填写或通过上传获得。

**上传结果**：远程配置上传成功后获得的配置链接。自动复制是否成功不改变上传成功的事实。

**UDP 选项**：转换时对 UDP 的要求，分为未指定、开启、关闭；未指定不等于关闭。
